[terminal]
vt = 1

[default_session]
# command = "cage -m last -s -- gtkgreet -l -c Hyprland"
# command = "cage -s -mlast -- regreet"
command = "dbus-run-session start-hyprland -- -c /etc/greetd/hyprland.lua"
user = "greeter"
